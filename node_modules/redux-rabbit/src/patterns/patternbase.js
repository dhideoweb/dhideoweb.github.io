export default class PatternBase {

  constructor( props ) {

  }

  attachClient( client ) {

    this.client = client;
  }

  configurePattern( args ) {

    this.injestionEndpoint = args.injestEndpoint;
    this.exchange = args.exchange;
  }
 
}
